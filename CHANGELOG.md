# 변경 이력

버전 마커는 `index.html` 헤더와 생성 로그에 표시됩니다. 아래는 대화 기록에서 재구성한 이력입니다.

## v3.4
- **달의 길이가 바뀔 때 차트가 깨지던 문제 수정(그래프 이상 표시의 실제 원인)**: `shiftRefs()`의 시트 내부 분기에는 일자범위 끝행(`3+cap`→`3+N`)을 확장하는 처리가 있었으나, **타 시트 분기(`cross`)와 차트 참조 경로에는 빠져 있었음**. 끝행이 `boundary(4+cap)`보다 작아 행 이동 대상에서 제외되기 때문. 결과:
  - 31일→30일(예: 5월→6월): 차트 범위가 `일별!$C$4:$C$34`로 남아 **합계행이 데이터 점으로 들어감** → 그래프 끝에 하루치의 30배짜리 값이 솟음
  - 30일→31일: 마지막 하루가 그래프에서 누락. 2월→3월은 3일 누락
  - 같은 길이의 달끼리는 정상이라 증상이 산발적으로 보였음
- 차트 참조 이동을 `shiftRefs(f,cap,N,true)` 호출로 통일(끝행 확장 포함). 6개 달 길이 전이 × 6개 참조 형태 = 36 케이스 검증 통과
- **기존 파일 복구(`healChartRanges`)**: v3.2 이전에 만들어진 마스터는 차트 범위가 이미 깨져 있고, 그대로 두면 delta 이동 때 한 칸씩 밀리기만 해서 버그가 다음 달로 계속 전파됨. 이동 **전에** 합계행·평균행까지 물고 있던 범위를 `4~(3+cap)`행으로 되돌려 전파를 끊음. 깨진 마스터 35개 케이스가 전부 정상 범위로 수렴함을 검증
- **자체검증 단계 추가(`validateOutput`)**: 생성 직후·다운로드 직전에 규칙으로 결과물을 훑고 경고를 로그에 출력. 경고가 있어도 **파일 생성은 계속함**(오탐으로 업무가 멈추지 않도록)
  - 차트의 일별 일자범위가 `4~3+N`행인지 (합계행 혼입·마지막 날 누락 탐지)
  - `[Content_Types].xml` 등록 누락 및 없는 파트를 가리키는 rels (엑셀 '복구가 필요합니다'의 주범)
  - 일별 시트 첫날·마지막날 serial, 합계행에 날짜가 남아있지 않은지
  - 14개 항목의 전월 대비 ±50% 초과 (원본 7개 파일 누락·오분류 시 여기서 잡힘)
  - multi 12개월 추이표의 이번 달 칸(V열) 공란 여부

## v3.3
- **토요일 공휴일 학습 버그 수정**: `harvestColors`가 마스터에서 "첫 토요일" 행을 무조건 평상시(파랑) 스타일로 가정 → 그 토요일이 공휴일과 겹쳐 이미 빨강으로 칠해져 있으면(예: 2026-06-06 현충일) 빨강을 토요일 기본색으로 잘못 학습해 다음 달 전체 토요일이 빨갛게 칠해짐
- 일요일 스타일을 먼저 확보한 뒤, 그 스타일과 다른(=공휴일이 아닌) 첫 토요일을 찾아서 채택하도록 수정(평일 스타일 추출 때 쓰던 검증 로직과 동일한 방식)

## v3.2
- **macOS에서 원본 7개 파일이 전혀 인식되지 않던 문제 수정**: macOS는 한글 파일명을 NFD(자모 분리)로 전달하는데 `classify()`가 소스의 NFC 리터럴과 `includes()` 비교를 해 7개 전부 매칭 실패 → 생성 버튼이 계속 비활성. 파일명을 `normalize("NFC")` 후 비교하도록 변경
- 같은 이유로 깨질 수 있는 **엑셀 컬럼명(집계일자·시간대 등)도 NFC 정규화** 추가(예방)
- 윈도우는 파일명을 NFC로 전달하므로 영향 없음. 실제 파일명 7종으로 검증(수정 전 0/7 → 수정 후 7/7)

## v3.1
- **일별 시트 날짜 색상에 공휴일 반영**: 토요일과 겹친 공휴일(예: 2026-06-06 현충일)을 파랑 대신 빨강으로 표시. 원본에서 수확한 일요일 서식을 재사용해 서식 일관성 유지
- 생성 직전 공휴일 캘린더를 `await`로 확보(실패해도 생성은 진행)
- 회귀 검증: 캘린더 없이 생성한 결과물이 v3.0과 완전 동일(차트 난수 GUID 제외)

## v3.0
- 공휴일을 **외부 캘린더 API(date.nager.at, 한국 KR)** 로 전환. 설날·추석·부처님오신날·대체공휴일까지 자동 반영
- 응답에서 **법정공휴일(Public 타입)만 필터**(제헌절 등 비공휴일 제외)
- 하드코딩 공휴일 목록 제거

## v2.9
- 하드코딩 대신 외부 공휴일 API 도입(초기 버전). 로드 완료 시 칩 색상 자동 갱신, 네트워크 실패 시 기존 동작으로 폴백

## v2.8
- 웹 화면 주말·공휴일 칩에서 **토요일 공휴일을 빨강으로** 표시(하드코딩 고정 공휴일 8종 기반). 이후 v2.9~v3.0에서 외부 API로 대체

## v2.7
- **비율누적 증감률(I열)을 14행 전부 수식으로 복원**. 원본 4월 이후 파일이 "빈 셀 참조 + 옛 캐시"로 망가져 재계산 시 0이 되던 문제 해결
- 통계파일용 D열 캐시를 활성 블록 기준으로 계산
- 검증: 원본 화면값(정규 +1.0%, 총매수 +3.0% 등 13/14 일치, 야간외래만 수동 입력 대상)

## v2.6
- **수식 캐시 주입**: 모든 수식 셀에 계산 결과를 캐시로 넣어, 보호된 보기·미리보기에서도 값이 보이게 함
- 일별 AA 행합계·합계/평균 행, 비율누적·통계·multi 셀 캐시 채움
- 버전 마커(헤더·로그) 추가

## 이전 세션 (v2.6 이전)

날짜별 버전 번호는 이 시점부터 도입됐고, 그 이전 작업은 다음 단위로 진행되었습니다:

- **UI Toss 스타일 리디자인**: 단계별 카드, 드롭존 로드 상태, 주말/공휴일 칩(토=파랑, 일/공휴일=빨강)
- **월 불일치 검증**: 마스터의 대상 월이 (목표−1월)과 다르면 생성 중단
- **일별 시트 행 재구성(reshape)**: 30일↔31일 마스터에 맞춰 행 삽입/삭제 + 참조 재조정, 합계·평균 행 재배치
- **비율누적 롤링**: 원작자의 "선준비 블록 활성화" 방식 역공학·구현
- **multi 시트**: 주말 블록 + J~V 12개월 추이표 + 전월 차트 복제
- **차트 복제 버그 수정**: 범위 참조 양끝 이동, 계열 GUID·앵커 creationId 재생성(중복 시 Excel 복구 삭제), 차트별 전용 colors/style 파트 생성
- **비근무일 처리**: 토·일·공휴일 및 15시 추가 ≤5건인 날의 09시·15시 자투리 0 처리
- 3개월(3·4·5월) 교차검증 하네스로 전 과정 검증
